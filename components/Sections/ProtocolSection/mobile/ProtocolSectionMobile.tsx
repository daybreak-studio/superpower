import React from "react";
import { Protocol } from "../Protocols";

type Props = {
  protocols: Protocol[];
};

const ProtocolSectionMobile = (props: Props) => {
  return <div>ProtocolSectionMobile</div>;
};

export default ProtocolSectionMobile;
